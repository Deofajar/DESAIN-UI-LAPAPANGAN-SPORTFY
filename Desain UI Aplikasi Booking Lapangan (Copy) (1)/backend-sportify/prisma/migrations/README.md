# Prisma migrations

Folder ini akan diisi otomatis ketika Anda menjalankan perintah migrasi Prisma (mis. `npx prisma migrate dev`).
Tambahkan file ini agar direktori tidak kosong di repository dan penjelasan "konten tidak ada" tidak muncul lagi.
